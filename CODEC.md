# Цифровой код руки — спецификация формата

Любой конфигурации робо-руки соответствует число. Код легко диктовать и
запоминать; в ссылке передаётся параметром `?c=` (полный код) или `?s=`
(структурный код). Реализация — `encodeArmCode` / `decodeArmCode` /
`decodeStructCode` в `index.html`.

## Структура кода

```
<версия> <компонент 1> <компонент 2> … <контрольная цифра>
```

- **Версия** — первая цифра, сейчас `1`. Любое несовместимое изменение
  формата (новые типы сверх десяти, другие диапазоны параметров) выпускается
  под новой версией; декодер обязан продолжать читать старые версии.
- **Компонент** — цифра-код типа, затем его параметры (в порядке из реестра
  `TYPES`), каждый — целое фиксированной ширины:
  `idx = round((value − min) / step)`, дополненное нулями слева.
  Обратно: `value = min + idx · step`.
- **Контрольная цифра** — алгоритм Дамма по всем цифрам кода (включая
  версию). Ловит любые одиночные опечатки и любые перестановки соседних
  цифр. Проверка: свёртка всех цифр вместе с контрольной даёт 0.
- **Разделители** — любые нецифровые символы допускаются и игнорируются.
  При формировании ссылки код бьётся дефисами по 4 цифры:
  `1022-5706-1170-3217-0921-9711-2290-275`.

## Структурный код (`?s=`)

Те же версия и контрольная цифра, но между ними — только цифры типов, без
параметров. Каждому параметру выставляется середина диапазона:
`value = min + round(steps / 2) · step`. Пример: `?s=1013-1240-3` —
yaw → pitch → link → pitch → roll → gripper → yaw.

## Таблица типов v1 — ЗАМОРОЖЕНА

Диапазоны, шаги и ширины полей — часть протокола: их изменение молча ломает
все разосланные ссылки. Менять что-либо в этой таблице можно **только
вместе с новой версией кода** (`CODE_VERSION`). Самопроверка
`checkCodeSpec()` при старте сверяет реестр `TYPES` с этой таблицей
(константа `CODE_V1_SPEC`) и ругается в консоль при расхождении.

| Код | Тип       | Параметр | min  | max | step | Ширина, цифр |
|----:|-----------|----------|-----:|----:|-----:|-------------:|
| 0   | yaw       | angle    | -180 | 180 | 1    | 3 |
| 1   | pitch     | angle    | -120 | 120 | 1    | 3 |
| 2   | roll      | angle    | -180 | 180 | 1    | 3 |
| 3   | link      | length   | 0.3  | 3   | 0.05 | 2 |
| 4   | gripper   | open     | 0    | 100 | 1    | 3 |
| 5   | prismatic | ext      | 0    | 1.2 | 0.05 | 2 |
| 6   | spherical | pitch    | -90  | 90  | 1    | 3 |
|     |           | yaw      | -180 | 180 | 1    | 3 |
| 7   | offset    | length   | 0.2  | 1.5 | 0.05 | 2 |
| 8   | rail      | pos      | -2   | 2   | 0.05 | 2 |
| 9   | suction   | power    | 0    | 100 | 1    | 3 |

Все десять цифр заняты — одиннадцатый тип потребует версии 2
(например, с двухзначными кодами типов).

## Пример разбора

`1022-5706-1170-3217-0921-9711-2290-275` → цифры `10225706117032170921971122902 75`:

- `1` — версия;
- `0 225` — yaw, angle = −180 + 225·1 = 45°;
- `7 06` — offset, length = 0.2 + 6·0.05 = 0.5;
- …и так далее до предпоследней цифры;
- последняя цифра — контрольная (Дамм).

## Совместимость

- `?c=` — полный цифровой код (основной формат с v1.3.0);
- `?s=` — структурный код;
- `?config=` — legacy: base64url(JSON), ссылки до v1.3.0 читаются по-прежнему;
- все параметры работают и во фрагменте (`#c=…`).

Битый код (не сошёлся Дамм, неизвестная версия или тип, оборванные
параметры) — рука стартует **пустой** с предупреждением, чтобы случайная
демо-рука не выдавала себя за содержимое ссылки.
